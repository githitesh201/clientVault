apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: clientvaultcrm-server-pvc
  namespace: clientvaultcrm
spec:
  storageClassName: default
  volumeName: clientvaultcrm-server-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
